---
"expo-build-disk-cache": patch
---

Load remote cache support only when configured and resolve providers from the consuming project without depending on private Expo CLI modules. Missing optional providers now include an installation command.

